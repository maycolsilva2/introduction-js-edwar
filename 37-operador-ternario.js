// El operador terniario en JS es una forma corta y elegante de una estructura if...else

    // se llama terniario porque tiene 3 operadores, observemos la sintaxis
    // condicion ? valorSiverdadero : valorSifalso

let age = 18;
let message = age >= 18 ? "eres mayor de edad" : "eres menor de edad"

console.log(message)